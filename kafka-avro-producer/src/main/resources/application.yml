server:
  port: 8080 
  
app:
  topic:
    name: equities
    
    
kafka:
  producer:
    bootstrap-servers: localhost:9092
    schema-registry-url: http://localhost:8081

management:
  endpoint:
    beans:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
        
